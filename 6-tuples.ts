const tuplesVariable: [number, string] = [10, "insertWord"];
const someOtherTupleVariable: [string, number] = ["wordshereSir", 53];

// it is illegal to format your tuple differently to how it was declared in the intiation! it must follow your inital format when declaring the variable.
// const LastTupleVariable: [string, number] = [53, "no u this is an illegal format, look how we did the first part!"]; // error will be returned

